{# templates/rdv/new.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}New RDV{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>New RDV</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary mt-3">Create RDV</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
