{# templates/rdv/show.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}RDV Details{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>RDV Details</h1>
        <p><strong>RDV Number:</strong> {{ rdv.numRdv }}</p>
        <p><strong>Date:</strong> {{ rdv.date }}</p>
        <a href="{{ path('rdv_index') }}" class="btn btn-primary">Back to RDVs</a>
    </div>
{% endblock %}
